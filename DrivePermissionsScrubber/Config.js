/**
 * @fileoverview Configuration switches
 */

var ROOT_FOLDER_ID = 'Folder Id'; 
var SHARED_FOLDER_OBJ = {'Folder Id 1': 'Domain 1', 'Folder Id 2': 'Domain 2'}; // specify permitted domains for sub-folders and their children
var NOTIFICATION_EMAIL = 'group@domain'; // email to notify of breaches
var REMOVE_FOLDER_PERMISSIONS = false; // false for testing

var REMOVE_FILE_PERMISSIONS = false;  // typically files within a non-shared folder may be shared as one-offs
var NOTIFY_FILE_ISSUES = false;
